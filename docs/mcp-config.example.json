{
  "_comment": "Example configuration for gmemory MCP Server. Prefer absolute command paths to avoid PATH issues in GUI clients.",
  "mcpServers": {
    "gmemory": {
      "command": "C:/ABSOLUTE/PATH/TO/gmemory-mcp",
      "args": [],
      "env": {
        "_comment": "Optional environment variables can be added here if needed"
      }
    }
  },
  "opencode_config_example": {
    "_comment": "For OpenCode, use absolute executable path in command array",
    "gmemory": {
      "command": [
        "C:/ABSOLUTE/PATH/TO/gmemory-mcp"
      ],
      "enabled": true,
      "type": "local"
    }
  }
}
